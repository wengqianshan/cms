extends layout

block content
    .container
        form(action='/upload', method='post')
            input(type='file', name='files', multiple)
            button(type='submit') 提交

block foot-js
    script.
        
                
