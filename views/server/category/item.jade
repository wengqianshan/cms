extends ../layout

block content
    .row
        .col-md-12
            .lists
                p 名称：#{category.name}
                p 标记(别名)：#{category.flag}
                p 父分类：#{category.parent.name}
                p 日期：#{category.created}
                p 描述：!{category.description}
