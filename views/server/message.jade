extends layout-blank

block content
    section#error-container
        .block-error
            .alert.alert-danger
                | #{msg}
                p
                a.btn.btn-info(href='#{adminDir}') 回首页
